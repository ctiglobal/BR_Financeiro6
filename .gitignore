# Harness de IA — overrides locais (não versionar)
.claude/settings.local.json
.claude/**/*.local.md

# Temporários / SO
Thumbs.db
Desktop.ini
*.tmp
*.log
# docs
*.pptx
*.xlsx

# TM1 / cargas locais (arquivos de dados não pertencem ao repo do modelo)
data/
*.csv
*.cma
