#region Prolog
# SDATA.ReprocessarFeeders
# Reprocessa (regera) os feeders do cubo informado em pCubeName.

IF( TRIM( pCubeName ) @= '' );
   ProcessQuit;
ENDIF;

IF( CubeExists( pCubeName ) = 0 );
   ProcessQuit;
ENDIF;

CubeProcessFeeders( pCubeName );
#endregion